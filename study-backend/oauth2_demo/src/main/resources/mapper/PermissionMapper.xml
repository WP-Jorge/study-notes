<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.oauth2_demo.mapper.PermissionMapper">
    <!--<cache type="com.example.oauth2_demo.cache.RedisCache"/>-->
    <cache-ref namespace="com.example.oauth2_demo.mapper.PermissionMapper"/>


    <select id="getPermissionByUsername" resultType="com.example.oauth2_demo.entity.Permission">
        select distinct permission
        from user, user_role, role, role_permission, permission
        where user.user_id = user_role.user_id
        and role.role_id = user_role.role_id
        and role.role_id = role_permission.role_id
        and role_permission.permission_id = permission.permission_id
        and username = #{username}
    </select>
</mapper>
